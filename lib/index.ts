import * as cdk from '@aws-cdk/core';

export interface SesVerifyIdentitiesProps {
  // Define construct properties here
}

export class SesVerifyIdentities extends cdk.Construct {

  constructor(scope: cdk.Construct, id: string, props: SesVerifyIdentitiesProps = {}) {
    super(scope, id);

    // Define construct contents here
  }
}
